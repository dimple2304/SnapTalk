<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>What's happening?</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
 <div class="flex min-h-screen bg-white">

    <div class="sidebar-partial-call hidden lg:block">
        <%- include('sidebar') %>
    </div>

    <main class="flex-1 max-w-3xl border-x border-slate-200">

        <header class="sticky top-0 z-10 flex items-center p-4 bg-white/80 backdrop-blur-sm border-b border-slate-200">
            <button class="p-2 rounded-full hover:bg-slate-100" onclick="window.history.back()">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-slate-700" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
              </svg>
            </button>
            <h2 class="ml-4 text-xl font-bold text-slate-800">Post</h2>
        </header>

        <article class="border-b border-slate-200">
            <!-- WORKING ON POST DISPLAY ON POST PAGE -->
                <%- include('postTemplate', {posts}) %>
        </article>

        <form class="flex space-x-4 p-4 border-b border-slate-200">
            <img class="h-10 w-10 rounded-full object-cover" src="<%= profile?.profilepic.url ? profile.profilepic?.url : 'https://placehold.co/100x100' %>" alt="Your Avatar">
            <div class="flex-1">
                <input type="text" class="w-full p-3 bg-slate-100 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" placeholder="Post your reply"></input>
                <div class="flex justify-end mt-2">
                    <button type="submit" class="py-2 px-6 rounded-full bg-indigo-600 text-white font-semibold hover:bg-indigo-700">
                        Reply
                    </button>
                </div>
            </div>
        </form>

        <section class="divide-y divide-slate-200">
            <% if(posts?.comments?.user){ %>
                <% posts.comments.forEach(element => { %>                 
                    <article class="flex space-x-4 p-4 hover:bg-slate-50 transition-colors">
                        <img class="h-10 w-10 rounded-full object-cover" src="https://placehold.co/40x40/ca8a04/ffffff?text=C" alt="Charlie's Avatar">
                        <div class="flex-1">
                            <div class="flex items-center space-x-2">
                                <h4 class="font-bold text-slate-800"><%= element.name %></h4>
                                <span class="text-sm text-slate-500">@<%= element.username %> · 1h</span>
                            </div>
                            <p class="mt-1 text-slate-700"><%= posts.comments.comment %></p>
                            <div class="mt-2 flex items-center space-x-4">
                                <button class="flex items-center space-x-1 text-slate-500 hover:text-pink-500">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 016.364 0L12 7.636l1.318-1.318a4.5 4.5 0 016.364 6.364L12 20.364l-7.682-7.682a4.5 4.5 0 010-6.364z"></path></svg>
                                    <span class="text-sm">5</span>
                                </button>
                            </div>
                        </div>
                    </article>
                <% }); %>
            <% } %>
        </section>

    </main> <%- include('rightColumn') %>

</div>
</body>

</html>