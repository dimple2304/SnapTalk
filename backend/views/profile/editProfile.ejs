<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile / SnapTalk</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    /* Custom styles for SweetAlert2 to match the theme */
    .swal2-popup {
      border-radius: 1rem !important;
    }
  </style>
</head>

<body class="bg-slate-100 flex items-center justify-center min-h-screen p-4">

  <!-- Hidden container for the modal's HTML content -->
  <div id="edit-profile-modal" class="hidden relative">
    <!-- Step 1: Profile Picture -->
    <div id="step-1" class="step-form flex flex-col justify-between h-[420px]">
      <div>
        <div class="text-center pt-8">
          <h2 class="text-2xl font-bold text-gray-900">Update Profile Picture</h2>
          <p class="mt-2 text-gray-500">Choose a new photo for your profile.</p>
        </div>
        <div class="mt-8 flex flex-col items-center">
          <img id="image-preview" src="https://placehold.co/128x128/e2e8f0/94a3b8?text=Preview" alt="Profile picture preview" class="w-32 h-32 rounded-full object-cover border-4 border-slate-200">
          <label for="profile-pic-input" class="mt-4 inline-block bg-slate-200 text-slate-800 font-semibold py-2 px-5 rounded-full cursor-pointer hover:bg-slate-300 transition-colors">
            Upload Photo
          </label>
          <input type="file" id="profile-pic-input" class="hidden" accept="image/*">
        </div>
      </div>
    </div>

    <!-- Step 2: Name Update -->
    <div id="step-2" class="step-form hidden flex flex-col justify-between h-[420px]">
      <div class="pt-8">
        <h2 class="text-2xl font-bold text-gray-900">Change Your Name</h2>
        <p class="mt-2 text-gray-500">This is how your name will appear on your profile.</p>
        <div class="mt-8">
          <label for="name-input" class="block mb-2 text-sm font-bold text-gray-700">Full Name</label>
          <input type="text" id="name-input" placeholder="Enter your full name" class="w-full px-4 py-3 bg-slate-100 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition">
        </div>
      </div>
    </div>

    <!-- Step 3: Username Update -->
    <div id="step-3" class="step-form hidden flex flex-col justify-between h-[420px]">
      <div class="pt-8">
        <h2 class="text-2xl font-bold text-gray-900">Update Your Username</h2>
        <p class="mt-2 text-gray-500">Your username must be unique.</p>
        <div class="mt-8">
          <label for="username-input" class="block mb-2 text-sm font-bold text-gray-700">Username</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <span class="text-gray-500 font-semibold">@</span>
            </div>
            <input type="text" id="username-input" placeholder="your_unique_username" class="w-full pl-7 pr-4 py-3 bg-slate-100 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition">
          </div>
        </div>
      </div>
    </div>

    <!-- Step 4: Bio Update -->
    <div id="step-4" class="step-form hidden flex flex-col justify-between h-[420px]">
      <div class="pt-8">
        <h2 class="text-2xl font-bold text-gray-900">Edit Your Bio</h2>
        <p class="mt-2 text-gray-500">Tell everyone a little bit about yourself.</p>
        <div class="mt-8">
          <label for="bio-input" class="block mb-2 text-sm font-bold text-gray-700">Bio</label>
          <textarea id="bio-input" rows="4" placeholder="Your bio..." class="w-full px-4 py-3 bg-slate-100 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition resize-none"></textarea>
        </div>
      </div>
    </div>

    <!-- Step 5: Link Update -->
    <div id="step-5" class="step-form hidden flex flex-col justify-between h-[420px]">
      <div class="pt-8">
        <h2 class="text-2xl font-bold text-gray-900">Add a Website Link</h2>
        <p class="mt-2 text-gray-500">Share a link to your personal site, portfolio, or other social media.</p>
        <div class="mt-8 space-y-4">
          <div>
            <label for="link-label-input" class="block mb-2 text-sm font-bold text-gray-700">Label</label>
            <input type="text" id="link-label-input" placeholder="e.g., My Portfolio" class="w-full px-4 py-3 bg-slate-100 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition">
          </div>
          <div>
            <label for="link-url-input" class="block mb-2 text-sm font-bold text-gray-700">URL</label>
            <input type="url" id="link-url-input" placeholder="https://example.com" class="w-full px-4 py-3 bg-slate-100 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 transition">
          </div>
        </div>
      </div>
    </div>
    <div class="absolute right-3 bottom-4 flex justify-between items-center">
      <button data-action="back" class="hidden font-semibold text-gray-600 hover:text-gray-900">Back</button>
      <button data-action="skip" class="bg-slate-200 text-slate-800 font-bold py-3 px-6 rounded-full hover:bg-slate-300 transition-colors">Skip</button>
      <button data-action="next" class="hidden bg-black text-white font-bold py-3 px-6 rounded-full hover:bg-slate-700 transition-colors">Next</button>
      <button id="save-changes" class="hidden bg-indigo-600 text-white font-bold py-3 px-6 rounded-full hover:bg-indigo-700 transition-colors">Save Changes</button>
    </div>
  </div>

  <script src="/js/editProfile.js"></script>
</body>

</html>